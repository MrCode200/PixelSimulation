slimes eat egg
when bat stands still slimy doesn't recognize maybe
food shroom and cactus
maybe add for each enemy in list slime move away(also makes sense when under stress they run more)
some slimes fight back (traits)

bug:
    vision collides with vision and not rect